function init(bot) {
  bot.command("/meet", (ctx) => {
    ctx.reply(
      "@Maks_Maryanchuk @varya_2908 @thermk \nУЖЕЕЕ В МІТ Доста фігньою заніматись!!!",
    );
  });
}

module.exports = { init };
